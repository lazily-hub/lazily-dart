name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        # Run every dart command from the package dir so the conformance tests'
        # sibling fixture path (`../lazily-spec/conformance/...`) resolves inside
        # the workspace.
        working-directory: lazily-dart
    strategy:
      fail-fast: false
      matrix:
        sdk: [stable, beta]
    # One run id per job invocation (#lzstalemanifest). CI does NOT run `make
    # check` — it reproduces the Makefile's steps — so the id the Makefile
    # generates never reaches these steps and has to be supplied here. Job
    # level, not step level: the `dart test` step must STAMP the same value the
    # two guard steps below REQUIRE, and a per-step id would be a different
    # value at every reference (the recursive-`=` mistake, in YAML).
    #
    # run_id + run_attempt + sdk is unique per invocation: a re-run of a failed
    # job bumps run_attempt, and the two matrix legs share a checkout path but
    # never a workspace.
    #
    # A runner cannot reach the state this guards against today — it starts from
    # a fresh workspace, so there is no leftover build/ to inherit — but the
    # stamp is what makes the CI steps and `make check` the SAME gate. Leaving
    # CI unstamped would mean the two guards refuse locally and pass on the
    # runner, which is the drift #lzcheckcireachguard exists to stop.
    env:
      LAZILY_CONFORMANCE_RUN_ID: ci-${{ github.run_id }}-${{ github.run_attempt }}-${{ matrix.sdk }}
    steps:
      # Check out lazily-dart into a subdirectory so its sibling repos
      # (`../lazily-spec`, `../lazily-formal`) can live beside it in the
      # workspace — `actions/checkout` cannot write above `$GITHUB_WORKSPACE`.
      - name: Checkout lazily-dart
        uses: actions/checkout@v4
        with:
          path: lazily-dart
      - uses: dart-lang/setup-dart@v1
        with:
          sdk: ${{ matrix.sdk }}


      # Sibling lazily-spec so the conformance tests (statechart / message-passing
      # / distributed / collections) resolve `../lazily-spec/conformance/...`
      # fixtures that are not copied into `test/conformance/`.
      - name: Checkout lazily-spec (sibling)
        uses: actions/checkout@v4
        with:
          repository: lazily-hub/lazily-spec
          path: lazily-spec

      # A checkout that succeeded but landed in the wrong place is invisible to
      # a skip-if-absent runner: the conformance tests would report green while
      # asserting nothing. Assert the sibling directories the tests actually
      # resolve, so the skip can never silently pass (#lzspecconf). Mirrors the
      # guard blocks in lazily-rs .github/workflows/ci.yml.
      - name: Assert canonical lazily-spec fixtures are present (#lzspecconf)
        run: |
          test -d ../lazily-spec/conformance || { echo "::error::../lazily-spec/conformance missing — the conformance tests would skip and report green"; exit 1; }
          # reactive-graph has NO bundled mirror under test/conformance/, so the
          # sibling is the only source: absent means the runner skips outright
          # and the async cascade fixture (c91a32a) goes unexecuted.
          test -d ../lazily-spec/conformance/reactive-graph || { echo "::error::../lazily-spec/conformance/reactive-graph missing — reactive_graph_conformance_test would skip and report green"; exit 1; }

      # Sibling lazily-formal Lean model so `dart test`'s formal_check_test builds
      # the proofs for real (instead of SKIP-ing). LAZILY_FORMAL_PATH points
      # tool/formal_check.dart at the checkout.
      - name: Checkout lazily-formal (sibling)
        uses: actions/checkout@v4
        with:
          repository: lazily-hub/lazily-formal
          path: _lazily-formal

      - name: Install Lean (elan)
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: _lazily-formal
          auto-config: false
          build: false
          test: false
          lint: false

      # lean-action installs elan but leaves it off PATH for the steps that
      # follow, and installs the toolchain without making it elan's default. Both
      # make `lake` unusable from tool/formal_check.dart, which SKIPs by design
      # when the toolchain is absent so pub.dev consumers and shallow clones still
      # run the Dart-only tests. On this runner that meant the Lean proofs were
      # never built and formal_check_test passed on the SKIP branch: a false green
      # the CI-reachability guard (#lzcheckcireachguard) surfaced.
      #
      # The default matters because formal_check probes with `lake --version` from
      # the package root, which has no lean-toolchain file — with no default, elan
      # has nothing to resolve and the probe fails exactly like a missing install.
      # Assert the shim before exporting it, so a moved install fails here rather
      # than reappearing as a silent skip.
      - name: Put the Lean toolchain on PATH (#lzcheckcireachguard)
        run: |
          test -x "$HOME/.elan/bin/lake" || { echo "::error::elan installed no lake at \$HOME/.elan/bin — formal-check would SKIP and prove nothing"; exit 1; }
          echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          "$HOME/.elan/bin/elan" default "$(cat "$GITHUB_WORKSPACE/_lazily-formal/lean-toolchain")"
          "$HOME/.elan/bin/lake" --version

      - run: dart pub get

      # The formatting gate (#lazilydartzig). Two placement constraints, both
      # learned the hard way when this step first went red in CI while `make fmt`
      # was green locally:
      #
      # 1. AFTER `dart pub get`, never before. `dart format` reads the package's
      #    language version from .dart_tool/package_config.json to choose between
      #    the pre-3.7 style and the 3.7+ tall style. With no package_config it
      #    silently picks the other one and reports 105 of 117 files "changed" on
      #    a tree that is clean — the same SDK version giving opposite verdicts
      #    purely on build state. A formatter that reads resolved metadata is a
      #    gate that needs a resolved workspace.
      # 2. `stable` ONLY. The SDK ships the formatter, so a beta SDK carries one
      #    this tree has never been run through; gating on beta would redden the
      #    build for an unreleased style change rather than for anything a
      #    contributor did. Beta still runs every other step.
      - name: Format gate (make fmt)
        if: matrix.sdk == 'stable'
        run: make fmt
      - run: dart analyze --fatal-infos

      # Two of the four conformance guards execute INSIDE `dart test` (the
      # unconsumed- and unasserted-assertion-key rungs, via the addTearDown in
      # test/conformance_assertions.dart) and two do not: the fixture-coverage
      # guard and the per-scenario replay ledger are read afterwards by
      # scripts/check-conformance-coverage.sh. CI ran the test binary and never
      # the script, so it enforced the first two and SILENTLY SKIPPED the other
      # two (#lzguardsnotinci). The steps below close that gap by reproducing
      # exactly what the Makefile's `test` + `conformance-coverage` targets do.
      #
      # Truncate the evidence files ONCE, here. The recorders APPEND — `dart
      # test` runs each test file in its own process and every one of them
      # contributes to a single union — so truncating anywhere else would either
      # scatter the evidence or destroy it mid-run. The `.lock` removals match
      # the Makefile: a lock left behind by a killed writer would stall every
      # subsequent append until its 10s deadline expires, once per record.
      - name: Reset conformance evidence files (#lzguardsnotinci)
        run: |
          mkdir -p build
          : > build/conformance-fixtures-loaded.txt
          rm -f build/conformance-fixtures-loaded.txt.lock
          : > build/conformance-scenarios-replayed.txt
          rm -f build/conformance-scenarios-replayed.txt.lock
          : > build/conformance-blocks-bound.txt
          rm -f build/conformance-blocks-bound.txt.lock

      - name: dart test (with lazily-formal proof verification)
        env:
          LAZILY_FORMAL_PATH: ${{ github.workspace }}/_lazily-formal
          # ABSOLUTE paths, the same values the Makefile passes as `$(CURDIR)/...`.
          # `dart test` runs each test file in its own process, and a relative
          # path would scatter partial evidence across whatever working directory
          # each process happens to use. Unset, the recorders are NO-OPS: the
          # guard below would then read empty files, and an evidence file that is
          # never written is the vacuous green this whole ladder exists to reject.
          LAZILY_CONFORMANCE_MANIFEST: ${{ github.workspace }}/lazily-dart/build/conformance-fixtures-loaded.txt
          LAZILY_CONFORMANCE_SCENARIOS: ${{ github.workspace }}/lazily-dart/build/conformance-scenarios-replayed.txt
          LAZILY_CONFORMANCE_BLOCKS: ${{ github.workspace }}/lazily-dart/build/conformance-blocks-bound.txt
        run: dart test

      # Two gates `make check` runs that CI never reached (#lzinteroppeerci). The
      # interop peer is the cross-binding wire-compatibility check — nine
      # bindings must agree on a byte layout — and the browser compile catches
      # accidental dart:io/VM-only dependencies creeping into the portable
      # stdlib. Both ran on a laptop and nowhere else, which is how they rot.
      - name: Interop peer self-check (#lzinteroppeerci)
        run: dart run bin/interop_peer.dart --self-check

      - name: stdlib browser compile check (#lzinteroppeerci)
        run: |
          mkdir -p build
          dart compile js -O1 -o build/stdlib_browser_check.js tool/stdlib_browser_check.dart
          node build/stdlib_browser_check.js

      # The step above covers `package:lazily/stdlib.dart` and nothing else, and
      # it passed the entire time the IPC wire did not compile to JavaScript at
      # all — dart2js refuses integer literals above 2^53 - 1, and the FNV-1a and
      # SplitMix64 constants were written as literals. This is the same gate over
      # the surface that carries the protocol, and it is the only place the
      # refuse-on-web half of #lzdartintwidth actually executes: on the VM that
      # branch is compiled out, so `dart test` can reach the policy and never the
      # platform binding.
      - name: IPC wire browser compile + behaviour check (#lzdartwebcompile)
        run: |
          mkdir -p build
          dart compile js -O1 -o build/ipc_browser_check.js tool/ipc_browser_check.dart
          node build/ipc_browser_check.js

      # Rungs 1 and 4: canonical-fixture coverage and per-scenario replay. Runs
      # AFTER the suite, never before — the script reads the evidence the run
      # just wrote, so ordering it first would only ever see the previous run's
      # manifest, or none at all.
      #
      # LAZILY_SPEC_CONFORMANCE_DIR is set explicitly rather than left to the
      # script's `../lazily-spec/conformance` default: the script SKIPs with exit
      # 0 when the corpus is absent, which on a runner would report green while
      # checking nothing. Naming the workspace path pins it to the checkout the
      # precondition step above already asserted, in the same spirit as that
      # guard block.
      - name: Conformance coverage + scenario replay guards (#lzguardsnotinci)
        env:
          LAZILY_SPEC_CONFORMANCE_DIR: ${{ github.workspace }}/lazily-spec/conformance
          LAZILY_CONFORMANCE_MANIFEST: ${{ github.workspace }}/lazily-dart/build/conformance-fixtures-loaded.txt
          LAZILY_CONFORMANCE_SCENARIOS: ${{ github.workspace }}/lazily-dart/build/conformance-scenarios-replayed.txt
        run: |
          # The scenario ledger check needs jq and fails closed without it, but a
          # guard that dies on a missing tool reads like a guard that found a
          # problem. Say which it is, up front.
          command -v jq >/dev/null 2>&1 || { echo "::error::jq is not installed on this runner — the scenario replay guard cannot read the corpus's scenario ids"; exit 1; }
          ./scripts/check-conformance-coverage.sh

      # Rung 6: the blocks inside the fixtures that were opened. The guards in
      # test/conformance_assertions.dart all reason about a block a runner
      # REACHED; this one names a block no runner ever bound, which every one of
      # them is blind to. It reads the ledger the suite just wrote, so it runs
      # after `dart test` for the same reason the coverage guard does.
      - name: Unbound assertion-block guard (#lzunboundblockguard)
        env:
          LAZILY_SPEC_CONFORMANCE_DIR: ${{ github.workspace }}/lazily-spec/conformance
          LAZILY_CONFORMANCE_MANIFEST: ${{ github.workspace }}/lazily-dart/build/conformance-fixtures-loaded.txt
          LAZILY_CONFORMANCE_BLOCKS: ${{ github.workspace }}/lazily-dart/build/conformance-blocks-bound.txt
        run: python3 scripts/check-unbound-blocks.py

      - name: Assertion observation ordering (#lzassertordering)
        run: python3 ../lazily-spec/scripts/check-assertion-ordering.py --binding dart --root .

      # `make check` runs `formal-check` as its own gate, and CI reached the Lean
      # proofs only through `dart test`'s formal_check_test — which accepts a SKIP
      # as a pass, by design, so pub.dev consumers and shallow clones still run.
      # On a runner that acceptance is exactly the vacuous green this ladder
      # rejects: a checkout that landed in the wrong place, or an elan install
      # that quietly failed, would print SKIP and the suite would stay green.
      # Run the target's own command and demand the verdict line.
      - name: Lean proof verification (#lzcheckcireachguard)
        env:
          LAZILY_FORMAL_PATH: ${{ github.workspace }}/_lazily-formal
        run: |
          # `case`, not `echo "$out" | grep -q` (#lzgrepcpipefail). `grep -q` exits
          # at its first hit; the upstream `echo` then takes SIGPIPE writing the
          # rest, and with `set -o pipefail` in effect the shell surfaces that as
          # the PIPELINE's status — so a HIT reads as a MISS. Measured in this
          # repo against the real 69512-byte block ledger: `PIPESTATUS=(141 0)`
          # in 15 of 40 runs, arming once the producer's output passes the 64KiB
          # pipe buffer. The SKIP test below is the dangerous direction — a match
          # means "I found an offender", so a missed hit is a FALSE GREEN, a
          # formal-check that skipped and was waved through.
          #
          # GitHub's default `run:` shell is `bash -e {0}`, so pipefail is OFF
          # here today and neither pipe is armed (measured: 40/40 hits). But that
          # is one `shell: bash` away — which ADDS `-o pipefail` — from arming
          # the false green, and `case` has no pipeline to poison at any size or
          # under any option. The guards in scripts/ use a here-string for the
          # same reason; see check-conformance-coverage.sh's covered-check.
          out=$(dart tool/formal_check.dart 2>&1) || { echo "$out"; echo "::error::lazily-formal Lean proofs failed to build"; exit 1; }
          echo "$out"
          case "$out" in
            *"SKIP —"*)
              echo "::error::formal-check skipped instead of building the Lean model — it proved nothing"
              exit 1 ;;
          esac
          case "$out" in
            *"OK — all Lean proofs in lazily-formal compile."*) ;;
            *)
              echo "::error::formal-check exited 0 without reaching its verdict"
              exit 1 ;;
          esac

      # The gate that keeps every step above honest (#lzcheckcireachguard). It
      # compares `make check`'s prerequisite closure against the run: bodies in
      # this file and fails on any target CI does not reach. Comments inside run:
      # bodies are stripped first — a naive grep for "make check" reported all
      # nine bindings as covered and every one of those hits was a comment.
      - name: CI-reachability guard (#lzcheckcireachguard)
        run: |
          # `case`, not a `grep -q` pipe, for the reason spelled out in the Lean
          # step above (#lzgrepcpipefail). This one is the fail-CLOSED direction —
          # a match means "the verdict I require is present", so a missed hit
          # would be a false RED — but the guard's own OK line is the only thing
          # standing between an exit 0 and a green badge, and it should not
          # depend on a pipe buffer either way.
          out=$(./scripts/check-ci-reach.sh 2>&1) || { echo "$out"; echo "::error::a make check gate is unreachable from CI"; exit 1; }
          echo "$out"
          case "$out" in
            *"check-ci-reach: OK"*) ;;
            *)
              echo "::error::the CI-reachability guard exited 0 without reaching its verdict"
              exit 1 ;;
          esac

  lean:
    name: Lean formal models (#lzleanformal)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout canonical lazily-spec
        uses: actions/checkout@v4
        with:
          repository: lazily-hub/lazily-spec
          path: lazily-spec

      - name: Checkout canonical lazily-formal
        uses: actions/checkout@v4
        with:
          repository: lazily-hub/lazily-formal
          path: lazily-formal

      - name: Build lazily-spec IPC formal model
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: lazily-spec/formal/lean
          auto-config: false
          build: true
          test: false
          lint: false

      - name: Build lazily-formal reactive + state-chart model
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: lazily-formal
          auto-config: false
          build: true
          test: false
          lint: false
