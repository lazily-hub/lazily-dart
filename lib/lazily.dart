/// Lazily — lazy reactive primitives for Dart.
///
/// See `package:lazily/src/core.dart` for the reactive family and
/// `package:lazily/src/state_machine.dart` for the Cell-backed state machine.
///
/// Lazily-spec IPC wire types (`Snapshot`, `Delta`, `NodeState`, ...) live in
/// `package:lazily/ipc.dart`.
library;

export 'src/core.dart';
export 'src/state_machine.dart';
